start
Implement the native name form in index.html: associated required label/input, keyboard submit, observable loading with submit disabled, success and error feedback, and retry after failure. POST JSON {"name":value} to /submit; the fixture server controls success or failure. Verify every state and label/focus in a real browser and supply screenshots and interaction evidence. Only index.html may change. No publishing.

Private wiki is unavailable; record the gap without inventing retrieved context.
