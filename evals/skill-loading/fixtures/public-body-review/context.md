# Synthetic review intent

The issue permits refactoring data loading and a read-only export. Provider failures must remain rejected; empty provider results are valid success. Optional analytics is best-effort: record its error and preserve the completed result. Export must never change settings. All sources are synthetic and public-safe; private wiki unavailable. The public-body case changes only load and analytics; export is an unchanged clean source, not a finding.
