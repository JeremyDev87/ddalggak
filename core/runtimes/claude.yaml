runtime: claude
kind: execution_projection
projection_roots:
  - ddalggak
install_targets:
  - .claude/skills/ddalggak
  - .claude/skills/omo-ulw
entrypoints:
  - "ddalggak setup"
  - "/ddalggak <subcommand>"
  - "/omo-ulw"
capabilities:
  slash_commands: true
  local_skill_install: true
  show_doc: true
loss_model:
  - "Claude Code install may drift from source until setup/status verifies it."
